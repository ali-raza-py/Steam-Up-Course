# 7TH CLASS — Mathematical Operations & String Processing

**Part of:** Phase 1: Foundations
> Classes 1–10 — Python fundamentals: variables, conditionals, loops, functions, and basic pattern/math logic.

## 🎯 Topics Covered / Learning Outcomes

- Practiced **Math functions**
- Practiced **String analysis**
- Practiced **Algorithm implementation**

## 📁 Files in This Folder

| File | Size | Purpose |
|---|---|---|
| `CUBE ROOT.py` | 92 B | Calculate cube roots |
| `Detect vowels.py` | 572 B | Identify vowels within text |
| `Float addition.py` | 135 B | Arithmetic with floating-point numbers |
| `HCF.py` | 198 B | Highest Common Factor calculation |

## 🧠 Code Explanation

Each script above is a small, self-contained program focused on the topic(s) listed. They're written to be run directly with `python "filename.py"` and generally follow this pattern: take some input (hard-coded or from the user), apply the concept being taught, and print/display the result. Reading them top-to-bottom is enough to follow the logic — there are no external project dependencies beyond what's noted below.

## ✅ How Others Can Benefit

- **Students on this course**: use this folder as a reference/checkpoint for "7TH CLASS — Mathematical Operations & String Processing" — compare your own solution before or after class.
- **Self-learners**: these scripts are small and self-contained, so you can read one file end-to-end in a few minutes to see Math functions, String analysis, Algorithm implementation applied in real code.
- **Quick lookup**: if you only need a working example of one of the topics above, open the matching file directly rather than reading the whole course in order.

## ▶️ Running the Code

```bash
python "path/to/file.py"
```

Some files in this phase may require: `pip install numpy pandas matplotlib seaborn dash --break-system-packages` (or without the flag, depending on your environment).
